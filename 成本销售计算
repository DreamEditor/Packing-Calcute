<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <title>极简利润核算系统</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; -webkit-tap-highlight-color: transparent; }
        input[type=number]::-webkit-inner-spin-button, input[type=number]::-webkit-outer-spin-button { -webkit-appearance: none; margin: 0; }
        .ios-card { background: white; border-radius: 16px; box-shadow: 0 4px 6px -1px rgba(0,0,0,0.05); padding: 16px; margin-bottom: 16px; }
        .ios-input { width: 100%; padding: 10px; background: #f3f4f6; border-radius: 8px; border: 1px solid transparent; font-size: 15px; outline: none; transition: all 0.2s; font-weight: 600;}
        .ios-input:focus { border-color: #3b82f6; background: #fff; }
        .ios-input::placeholder { color: #a1a1aa; font-weight: normal; }
        .compact-label { font-size: 12px; color: #6b7280; margin-bottom: 6px; display: block; }
    </style>
</head>
<body class="bg-gray-100 pb-48">

<div class="max-w-md mx-auto p-4 pt-6">
    <h1 class="text-2xl font-bold text-gray-800 mb-6 px-2">产品出厂利润核算</h1>

    <div class="ios-card border-l-4 border-orange-400">
        <h2 class="text-base font-bold text-gray-700 mb-4 border-b pb-2 flex justify-between">
            1. 内容物成品成本
            <span class="text-orange-600"><span id="res-c-content">0.000</span> Tk/包</span>
        </h2>
        
        <div class="bg-orange-50 p-3 rounded-lg mb-4 border border-orange-100">
            <div class="text-[11px] text-orange-800 font-bold mb-2">每公斤 (1000g) 混合配方成本:</div>
            <div class="grid grid-cols-3 gap-2 mb-3">
                <div>
                    <label class="text-[10px] text-gray-500 block mb-1">棕油 40% (400g)</label>
                    <input type="number" id="p-oil" class="ios-input text-sm py-1.5 px-2" value="180" oninput="calc()">
                </div>
                <div>
                    <label class="text-[10px] text-gray-500 block mb-1">调料 10% (100g)</label>
                    <input type="number" id="p-sea" class="ios-input text-sm py-1.5 px-2" value="400" oninput="calc()">
                </div>
                <div>
                    <label class="text-[10px] text-gray-500 block mb-1">坯料 50% (500g)</label>
                    <input type="number" id="p-pel" class="ios-input text-sm py-1.5 px-2" value="68" oninput="calc()">
                </div>
            </div>
            <div class="flex justify-between items-center border-t border-orange-200 pt-2">
                <span class="text-xs text-orange-800">核算成品均价：</span>
                <span class="font-black text-orange-600 text-lg"><span id="res-avg-price">146.0</span> <span class="text-xs font-normal">Tk/kg</span></span>
            </div>
        </div>

        <div>
            <label class="compact-label font-bold text-gray-700">手工填写：单包克重 (g)</label>
            <input type="number" id="c-weight" class="ios-input text-orange-600 border-orange-200 bg-white shadow-sm" placeholder="直接输入单包重量，如: 12" oninput="calc()">
        </div>
    </div>

    <div class="ios-card border-l-4 border-blue-400">
        <h2 class="text-base font-bold text-gray-700 mb-4 border-b pb-2">2. 内包装成本</h2>
        <div>
            <label class="compact-label">单个包装袋成本 (Taka/包)</label>
            <input type="number" id="i-cost" class="ios-input text-blue-600" placeholder="直接输入单包包装费" oninput="calc()">
        </div>
    </div>

    <div class="ios-card border-l-4 border-cyan-500">
        <h2 class="text-base font-bold text-gray-700 mb-4 border-b pb-2 flex justify-between">
            3. 外包装塑料袋成本
            <span class="text-cyan-600"><span id="res-c-outer">0.000</span> Tk/包</span>
        </h2>
        <div class="text-xs text-gray-500 mb-3 bg-gray-50 p-2 rounded">
            设定：塑料袋统一定价 200 Taka/kg。<br>架构：1大袋 装 10中袋，1中袋 装 20小包。
        </div>
        <div class="grid grid-cols-2 gap-4">
            <div>
                <label class="compact-label">单条中包重量 (g)</label>
                <input type="number" id="o-med-wt" class="ios-input text-cyan-700" placeholder="空" oninput="calc()">
            </div>
            <div>
                <label class="compact-label">单个大包重量 (g)</label>
                <input type="number" id="o-lrg-wt" class="ios-input text-cyan-700" placeholder="空" oninput="calc()">
            </div>
        </div>
    </div>

    <div class="ios-card border-l-4 border-green-500">
        <h2 class="text-base font-bold text-gray-700 mb-4 border-b pb-2 flex justify-between">
            4. 规格与运输成本
            <span class="text-green-600"><span id="res-c-trans">0.000</span> Tk/包</span>
        </h2>
        <div class="grid grid-cols-2 gap-4">
            <div>
                <label class="compact-label">每件数量 (小包/大包)</label>
                <input type="number" id="t-qty" class="ios-input" placeholder="空 (默认200)" oninput="calc()">
            </div>
            <div>
                <label class="compact-label text-green-700">单件/大包 运费 (Taka)</label>
                <input type="number" id="t-cost" class="ios-input text-green-700 bg-green-50" placeholder="空" oninput="calc()">
            </div>
        </div>
    </div>

    <div class="ios-card border-l-4 border-purple-500">
        <h2 class="text-base font-bold text-gray-700 mb-4 border-b pb-2 flex justify-between">
            5. 运营固定成本
            <span class="text-purple-600"><span id="res-c-fixed">0.000</span> Tk/包</span>
        </h2>
        <div class="grid grid-cols-2 gap-4">
            <div>
                <label class="compact-label">日固定成本 (Taka)</label>
                <input type="number" id="f-cost" class="ios-input" value="10000" oninput="calc()">
            </div>
            <div>
                <label class="compact-label">预估日产量 (件/大包)</label>
                <input type="number" id="f-vol" class="ios-input text-purple-700" value="167" oninput="calc()">
                <div class="text-[10px] text-gray-400 mt-1">均摊计算所需产量</div>
            </div>
        </div>
    </div>

    <div class="ios-card border-l-8 border-emerald-500 shadow-md">
        <h2 class="text-base font-black text-gray-800 mb-3 border-b border-gray-200 pb-2 flex justify-between items-center">
            6. 最终出厂报价
            <span class="text-emerald-600 font-bold"><span id="res-s-small">0.00</span> Tk/包</span>
        </h2>
        <div>
            <label class="compact-label font-bold text-gray-600">单件 (大包) 出厂价 (Taka)</label>
            <input type="number" id="s-price" class="ios-input bg-white text-gray-900 border-2 border-emerald-400 text-2xl text-center placeholder-gray-300 py-4 font-black shadow-sm" placeholder="输入单件销售金额" oninput="calc()">
        </div>
    </div>

</div>

<div class="fixed bottom-0 left-0 right-0 bg-white shadow-[0_-10px_20px_-5px_rgba(0,0,0,0.15)] pb-safe pt-3 px-4 rounded-t-3xl z-50 max-w-md mx-auto" style="padding-bottom: env(safe-area-inset-bottom);">
    
    <div class="flex justify-between items-center mb-3 px-1 border-b pb-2 border-gray-100">
        <div class="flex flex-col">
            <span class="text-gray-500 text-[11px] font-medium">单小包总成本</span>
            <div class="text-lg font-black text-gray-800"><span id="final-cost-small">0.000</span> <span class="text-xs font-normal">Tk</span></div>
        </div>
        <div class="flex flex-col items-end">
            <span class="text-gray-500 text-[11px] font-medium">单件(大包)总成本</span>
            <div class="text-lg font-black text-gray-800"><span id="final-cost-large">0.00</span> <span class="text-xs font-normal">Tk</span></div>
        </div>
    </div>

    <div class="grid grid-cols-2 gap-3 mb-4">
        <div class="bg-emerald-50 rounded-xl p-2 pl-3 relative border border-emerald-100">
            <div class="text-[10px] text-emerald-600 mb-0.5 font-bold tracking-wide">单件利润 (Taka)</div>
            <div class="text-xl font-black text-emerald-700"><span id="final-profit">0.00</span></div>
            <div class="absolute top-2 right-2 text-xs font-bold text-emerald-600 bg-emerald-100 px-1.5 py-0.5 rounded"><span id="final-margin">0.0</span>%</div>
        </div>
        <div class="bg-blue-50 rounded-xl p-2 pl-3 border border-blue-100">
            <div class="text-[10px] text-blue-600 mb-0.5 font-bold tracking-wide">保本销量 (件/天)</div>
            <div class="text-xl font-black text-blue-700"><span id="final-breakeven">0</span></div>
            <div class="text-[9px] text-gray-500 mt-1">收支平衡点</div>
        </div>
    </div>
</div>

<script>
    function v(id) { 
        // 允许获取为空的情况，如果有默认计算则在主逻辑处理
        const val = document.getElementById(id).value;
        return val === "" ? 0 : parseFloat(val); 
    }

    function calc() {
        // --- 每件数量基数设定 ---
        let qtyRaw = document.getElementById('t-qty').value;
        const qty = qtyRaw === "" ? 200 : (parseFloat(qtyRaw) || 1); 

        // 1. 自动配方核算与内容物成本
        const oilPrice = v('p-oil');
        const seaPrice = v('p-sea'); // 即使用户清空了也会返回0参与计算
        const pelPrice = v('p-pel');

        // 核心配方算法：(400g油 + 100g调料 + 500g坯料) / 1000g = 1kg均价
        const avgPrice = (0.4 * oilPrice) + (0.1 * seaPrice) + (0.5 * pelPrice);
        document.getElementById('res-avg-price').innerText = avgPrice.toFixed(1);

        const cWeight = v('c-weight');
        const costContentSmall = (cWeight / 1000) * avgPrice;
        document.getElementById('res-c-content').innerText = costContentSmall.toFixed(3);

        // 2. 内包装成本
        const costInnerSmall = v('i-cost');

        // 3. 外包装塑料袋成本 (单价固定为 200 Tk/kg)
        const medWt = v('o-med-wt');
        const lrgWt = v('o-lrg-wt');
        const costOuterLarge = (10 * (medWt / 1000) * 200) + (1 * (lrgWt / 1000) * 200);
        const costOuterSmall = costOuterLarge / qty;
        document.getElementById('res-c-outer').innerText = costOuterSmall.toFixed(3);

        // 4. 运输成本分摊
        const transCostLarge = v('t-cost');
        const transCostSmall = transCostLarge / qty;
        document.getElementById('res-c-trans').innerText = transCostSmall.toFixed(3);

        // 5. 运营固定成本分摊
        const fixedDaily = v('f-cost');
        let dailyVol = v('f-vol');
        if(dailyVol <= 0) dailyVol = 1; 
        
        const fixedCostLarge = fixedDaily / dailyVol;
        const fixedCostSmall = fixedCostLarge / qty;
        document.getElementById('res-c-fixed').innerText = fixedCostSmall.toFixed(3);

        // --- 汇总计算 ---
        const totalCostSmall = costContentSmall + costInnerSmall + costOuterSmall + transCostSmall + fixedCostSmall;
        const totalCostLarge = totalCostSmall * qty;
        
        document.getElementById('final-cost-small').innerText = totalCostSmall.toFixed(3);
        document.getElementById('final-cost-large').innerText = totalCostLarge.toFixed(1);

        // 6. 销售价格换算
        const salesPriceLarge = v('s-price');
        const salesPriceSmall = salesPriceLarge > 0 ? (salesPriceLarge / qty) : 0;
        document.getElementById('res-s-small').innerText = salesPriceSmall.toFixed(2);

        // 7. 利润率与收支平衡计算
        const varCostLarge = totalCostLarge - fixedCostLarge;
        const contributionMargin = salesPriceLarge - varCostLarge;

        let breakeven = 0;
        if (contributionMargin > 0) {
            breakeven = Math.ceil(fixedDaily / contributionMargin);
        }

        const profitLarge = salesPriceLarge - totalCostLarge;
        const margin = salesPriceLarge > 0 ? (profitLarge / salesPriceLarge) * 100 : 0;

        // --- UI 渲染更新 ---
        const elProfit = document.getElementById('final-profit');
        elProfit.innerText = salesPriceLarge > 0 ? profitLarge.toFixed(1) : "0.00";
        elProfit.className = profitLarge >= 0 ? "text-xl font-black text-emerald-700" : "text-xl font-black text-red-600";
        
        const elMargin = document.getElementById('final-margin');
        elMargin.innerText = salesPriceLarge > 0 ? margin.toFixed(1) : "0.0";
        elMargin.parentElement.className = profitLarge >= 0 ? "absolute top-2 right-2 text-xs font-bold text-emerald-600 bg-emerald-100 px-1.5 py-0.5 rounded" : "absolute top-2 right-2 text-xs font-bold text-red-600 bg-red-100 px-1.5 py-0.5 rounded";

        document.getElementById('final-breakeven').innerText = contributionMargin > 0 ? breakeven.toLocaleString() : (salesPriceLarge > 0 ? "亏损" : "0");
    }

    window.onload = calc;
</script>
</body>
</html>
